import React from "react";

import "./Footer.css";

const Footer = (props) => {
  return (
    <footer>
      <input type="text" placeholder="Send a message" />
    </footer>
  );
};

export default Footer;
